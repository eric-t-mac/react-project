
const initState = {
    list: [
        {id: 1, name: '张杰'},
        {id: 2, name: 'zhou'},
        {id: 3, name: 'c'},
        {id: 4, name: 'a'}
    ]
}

export default function musicReducer(state=initState, action) {
    switch(1) {
        case '':
            break;
        default: 
    }
    return state;
}